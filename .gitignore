# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env / secrets — never commit (portfolio standard, root CLAUDE.md)
.env
.env.*
*.env
*service-account*.json
*-service-account.json
firebase-adminsdk*.json
secrets/

# Machine-local Claude Code permissions (*.local does not match a .json suffix)
.claude/settings.local.json

# Firebase deploy cache — regenerated by every deploy; tracking it produced
# junk commits, a permanent phantom-dirty entry under core.autocrlf, and a
# OneDrive conflict copy when both machines deployed.
.firebase/
